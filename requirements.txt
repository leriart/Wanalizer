# Core runtime dependencies (also declared in pyproject.toml).
# This file exists for users who prefer `pip install -r requirements.txt`
# over `pip install .`. The canonical source of truth is pyproject.toml.
Pillow>=9.0.0
numpy>=1.24.0
requests>=2.28.0

# GUI
PySide6>=6.6.0

# Optional: classical CV support (KMeans palette, perceptual hashes,
# OpenCV edge/contour helpers). Recommended for LowLevel mode.
# opencv-python-headless>=4.8.0
# imagehash>=4.3.0
# scikit-learn>=1.3.0

# Optional: CLIP mode (CPU build, ~2 GB).
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# pip install ftfy regex
# pip install git+https://github.com/openai/CLIP.git

# Optional: extra image format plugins (AVIF, HEIC, JXL, PSD, RAW, SVG, PDF).
# pillow-avif-plugin>=1.4.0
# pillow-heif>=0.16.0
# pillow-jxl-plugin>=1.3.0
# psd-tools>=1.10.0
# OpenEXR>=3.2.0
# cairosvg>=2.7.0
# pypdfium2>=4.0.0
# rawpy>=0.18.0

# Dev / test extras (or: pip install ".[dev]").
# pytest>=7.0
# pytest-cov>=4.0
# ruff>=0.4.0

# External system tools (not pip-installable).
# ffmpeg >= 4.0  (single-frame extraction from videos / animated images)