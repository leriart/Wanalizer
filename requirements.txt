# Core dependencies
Pillow>=9.0.0
numpy>=1.24.0
requests>=2.28.0

# GUI
PySide6>=6.6.0

# --- Optional (recommended for low-level CV mode) ---
# opencv-python-headless>=4.8.0    (edge detection, features, contours)
# imagehash>=4.3.0                 (perceptual hashing)
# scikit-learn>=1.3.0              (KMeans palette)

# --- Optional: CLIP mode ---
# torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu
# torchvision>=0.15.0 --index-url https://download.pytorch.org/whl/cpu
# git+https://github.com/openai/CLIP.git

# --- Optional: format plugins ---
# pillow-avif-plugin>=1.4.0
# pillow-heif>=0.16.0
# pillow-jxl-plugin>=1.3.0
# psd-tools>=1.10.0
# OpenEXR>=3.2.0
# cairosvg>=2.7.0
# pypdfium2>=4.0.0
# rawpy>=0.18.0

# --- External tools (system packages) ---
# ffmpeg >= 4.0  (video support)
